---
import BaseLayout from '../../layouts/BaseLayout.astro';
const allPosts = await Astro.glob('../../**/*.md');
const posts = allPosts
  .filter(p => p.frontmatter.category === 'community')
  .sort((a, b) => new Date(b.frontmatter.date) - new Date(a.frontmatter.date));
---
<BaseLayout title="Community — Mahjo" description="Club spotlights, player stories, and Mahjong culture.">
  <div class="category-header">
    <div class="container--narrow">
      <span class="category-header__icon">🤝</span>
      <h1>Community</h1>
      <p style="color: var(--text3); margin-top: 0.75rem;">Club spotlights, player stories, and Mahjong culture.</p>
    </div>
  </div>
  <div class="container" style="padding-bottom: 5rem;">
    {posts.length === 0 ? (
      <p class="text-center text-muted" style="padding: 3rem 0;">Articles coming soon.</p>
    ) : (
      <div class="card-grid">
        {posts.map(post => (
          <a href={post.url} class="card">
            <h3 class="card__title">{post.frontmatter.title}</h3>
            <p class="card__excerpt">{post.frontmatter.description}</p>
            <span class="card__read-more">Read →</span>
          </a>
        ))}
      </div>
    )}
  </div>
</BaseLayout>
